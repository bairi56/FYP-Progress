from django.urls import path
from .views import QuestionGenerationView

urlpatterns = [
    # path('generate_questions/', QuestionGenerationView.as_view(), name='generate_questions'),
    # path("api/", fastapi_app),

]
